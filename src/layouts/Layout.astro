---

interface Props {
title: string;
}

## const { title } = Astro.props;

<!doctype html>

<html lang="en">
  <head>
    <meta charset="UTF-8" />

```
<title>{title} | Software Developer</title>

<meta
  name="description"
  content="Aymane Jabrane - Software Developer building interactive and seamless digital experiences."
/>

<meta
  name="viewport"
  content="width=device-width, initial-scale=1.0"
/>

<link rel="icon" type="image/png" href="/icon.png" />

<link
  rel="canonical"
  href="https://aymanejab01.github.io/"
/>

<link
  rel="preload"
  href="/fonts/montserrat-latin-wght-normal.woff2"
  as="font"
  type="font/woff2"
  crossorigin="anonymous"
/>

<meta
  property="og:title"
  content="Aymane Jabrane | Software Developer"
/>

<meta
  property="og:description"
  content="Portfolio of Aymane Jabrane, Software Developer interested in frontend development, machine learning, hardware, software, and computer architecture."
/>

<meta
  property="og:image"
  content="https://aymanejab01.github.io/aj2.png"
/>

<meta
  property="og:url"
  content="https://aymanejab01.github.io"
/>

<meta property="og:type" content="website" />

<meta
  property="og:site_name"
  content="Aymane Jabrane Portfolio"
/>

<!-- Structured Data -->
<script is:inline type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "Person",
    "name": "Aymane Jabrane",
    "url": "https://aymanejab01.github.io",
    "image": "https://aymanejab01.github.io/og.image.png",
    "sameAs": [
      "https://github.com/AymaneJab01",
      "https://www.linkedin.com/in/aymane-jabrane-73025726a/"
    ],
    "jobTitle": "Software Developer",
    "description": "Software Developer interested in frontend development, machine learning, hardware, software, and computer architecture."
  }
</script>
```

  </head>

  <body class="bg-[var(--background)] sm:px-28 lg:px-20 px-9">
    <slot />
  </body>
</html>

<style is:global>
  /* ========================================================= */
  /* FONT */
  /* ========================================================= */

  @font-face {
    font-family: "Montserrat Variable";
    src: url("/fonts/montserrat-latin-wght-normal.woff2")
      format("woff2-variations");
    font-weight: 100 900;
    font-style: normal;
    font-display: swap;
  }


  /* ========================================================= */
  /* GLOBAL COLOR SYSTEM */
  /* ========================================================= */

  :root {
    /*
     * Main background
     * Deep blue-black rather than pure black.
     */
    --background: #07111f;

    /*
     * Electric blue primary accent.
     */
    --sec: #00aaff;

    /*
     * Stronger electric blue.
     */
    --electric-blue: #008cff;

    /*
     * Bright cyan highlight.
     */
    --electric-cyan: #19c8ff;

    /*
     * Main white.
     */
    --white: #f8fbff;

    /*
     * Secondary white.
     */
    --white-icon: rgba(235, 245, 255, 0.72);

    /*
     * Very subtle borders.
     */
    --white-icon-tr: rgba(235, 245, 255, 0.11);

    /*
     * Weather-dashboard surfaces.
     */
    --container: #0c1a2b;

    --container-light: #10243a;

    /*
     * Blue glass.
     */
    --blue-glass: rgba(0, 140, 255, 0.08);

    /*
     * Blue border.
     */
    --blue-border: rgba(0, 170, 255, 0.24);

    /*
     * Blue glow.
     */
    --blue-glow: rgba(0, 170, 255, 0.16);
  }


  /* ========================================================= */
  /* GLOBAL RESET */
  /* ========================================================= */

  *,
  *::before,
  *::after {
    font-family:
      "Montserrat Variable",
      -apple-system,
      BlinkMacSystemFont,
      "Segoe UI",
      system-ui,
      sans-serif;

    box-sizing: border-box;

    padding: 0;
    margin: 0;
  }


  html {
    background: var(--background);

    scroll-behavior: smooth;
  }


  body {
    min-height: 100vh;

    background:
      radial-gradient(
        circle at 50% -10%,
        rgba(0, 170, 255, 0.10),
        transparent 38%
      ),
      var(--background);

    color: var(--white);

    text-rendering: optimizeLegibility;

    -webkit-font-smoothing: antialiased;
  }


  /* ========================================================= */
  /* SELECTION */
  /* ========================================================= */

  *::selection {
    background-color: var(--sec);

    color: #00111f;
  }


  /* ========================================================= */
  /* LINKS */
  /* ========================================================= */

  a {
    color: inherit;

    text-decoration: none;
  }


  /* ========================================================= */
  /* SCROLLBAR */
  /* ========================================================= */

  ::-webkit-scrollbar {
    width: 10px;

    height: 10px;
  }


  ::-webkit-scrollbar-track {
    background:
      #06101d;
  }


  ::-webkit-scrollbar-thumb {
    background:
      rgba(0, 170, 255, 0.45);

    border-radius:
      999px;

    border:
      2px solid #06101d;
  }


  ::-webkit-scrollbar-thumb:hover {
    background:
      var(--sec);
  }


  * {
    scrollbar-width: thin;

    scrollbar-color:
      rgba(0, 170, 255, 0.5)
      #06101d;
  }


  /* ========================================================= */
  /* WEATHER / DASHBOARD UTILITIES */
  /* ========================================================= */

  /*
   * These utility classes are optional.
   * They do not interfere with your existing pages.
   */

  .weather-surface {
    background:
      linear-gradient(
        135deg,
        rgba(255, 255, 255, 0.97),
        rgba(238, 248, 255, 0.96)
      );

    color:
      #07111f;

    border:
      1px solid
      rgba(0, 140, 255, 0.15);

    box-shadow:
      0 20px 60px
      rgba(0, 30, 70, 0.18);
  }


  .weather-blue-surface {
    background:
      linear-gradient(
        135deg,
        #007eea,
        #00aaff
      );

    color:
      white;

    box-shadow:
      0 20px 60px
      rgba(0, 140, 255, 0.22);
  }


  .weather-glow {
    box-shadow:
      0 0 0 1px
      rgba(0, 170, 255, 0.06),

      0 20px 70px
      rgba(0, 140, 255, 0.12);
  }


  /* ========================================================= */
  /* REDUCED MOTION */
  /* ========================================================= */

  @media (prefers-reduced-motion: reduce) {

    html {
      scroll-behavior: auto;
    }

    *,
    *::before,
    *::after {
      animation-duration: 0.01ms !important;

      animation-iteration-count: 1 !important;

      transition-duration: 0.01ms !important;

      scroll-behavior: auto !important;
    }
  }


  /* ========================================================= */
  /* MOBILE */
  /* ========================================================= */

  @media (max-width: 640px) {

    body {
      background:
        radial-gradient(
          circle at 50% -5%,
          rgba(0, 170, 255, 0.08),
          transparent 42%
        ),
        var(--background);
    }
  }
</style>
