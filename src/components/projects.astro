---

---

<section
  id="projects"
  class="relative overflow-hidden text-[var(--white)] py-24 md:py-36"
>

  <!-- ========================================================= -->
  <!-- BACKGROUND — AURORA (Momiji Red / Forest Green) -->
  <!-- ========================================================= -->

  <div class="pointer-events-none absolute inset-0 -z-10 overflow-hidden bg-[#07070b]">
    <div class="nebula-field-alt" aria-hidden="true">
      <div class="nebula-ribbon-alt ribbon-back-alt"></div>
      <div class="nebula-ribbon-alt ribbon-mid-alt"></div>
      <div class="nebula-ribbon-alt ribbon-core-alt"></div>
      <div class="nebula-glow-alt glow-a-alt"></div>
      <div class="nebula-glow-alt glow-b-alt"></div>
    </div>

    <div class="absolute inset-0 hero-dots-alt opacity-[0.5]"></div>

    <div class="absolute inset-0 noise-layer-alt opacity-[0.02]"></div>

    <div class="hero-vignette-alt"></div>
  </div>

  <div class="max-w-6xl mx-auto px-6 relative z-10">

    <!-- ========================================================= -->
    <!-- SECTION HEADER -->
    <!-- ========================================================= -->

    <div class="mb-16 md:mb-20">
      <div class="flex items-center gap-3 mb-5">
        <span
          class="h-2 w-2 rounded-full bg-[var(--sec)]
          shadow-[0_0_12px_var(--sec)]"
        ></span>

        <span
          class="text-sm tracking-[0.25em] uppercase
          text-[var(--sec)] shiny-sec"
        >
          Projects
        </span>
      </div>

      <h2
        class="text-4xl md:text-6xl font-medium tracking-tight
        text-[var(--white)]"
      >
        Things I've built<span class="text-[var(--sec)]">.</span>
      </h2>

      <p
        class="mt-5 max-w-2xl text-lg
        text-[var(--white-icon)] leading-relaxed"
      >
        A collection of projects, experiments and ideas that
        represent what I'm building and learning as a software developer.
      </p>
    </div>


    <!-- ========================================================= -->
    <!-- PROJECT 01 — TERMINAL -->
    <!-- ========================================================= -->

    <div
      class="group relative mb-10 overflow-hidden rounded-2xl
      border border-[var(--white-icon-tr)]
      bg-[#1414149c] backdrop-blur-sm
      transition-all duration-500
      hover:border-[var(--sec)]/40
      hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
    >

      <div
        class="pointer-events-none absolute -right-32 -top-32
        h-64 w-64 rounded-full
        bg-[var(--sec)] opacity-[0.04] blur-3xl
        transition-opacity duration-500
        group-hover:opacity-[0.08]"
      ></div>

      <a
        href="/#terminal"
        class="relative block p-7 md:p-10"
      >

        <div class="flex flex-col md:flex-row md:items-start md:justify-between gap-8">

          <div class="flex-1">

            <div class="mb-5 flex items-center gap-3">
              <span
                class="font-mono text-xs tracking-[0.2em]
                text-[var(--sec)]"
              >
                PROJECT 01
              </span>

              <span
                class="h-px w-12 bg-[var(--sec)] opacity-40"
              ></span>

              <span
                class="text-xs uppercase tracking-widest
                text-[var(--white-icon)]"
              >
                Interactive
              </span>
            </div>

            <h3
              class="text-2xl md:text-4xl font-medium
              tracking-tight text-[var(--white)]
              transition-colors duration-300
              group-hover:text-[var(--sec)]"
            >
              Developer Terminal
              <span class="text-[var(--sec)]">.</span>
            </h3>

            <p
              class="mt-4 max-w-2xl text-base md:text-lg
              leading-relaxed text-[var(--white-icon)]"
            >
              An interactive terminal built directly into my portfolio,
              designed to provide a developer-style way of exploring
              my work, skills and information.
            </p>

            <p
              class="mt-3 max-w-2xl text-sm
              leading-relaxed text-[var(--white-icon)]
              opacity-70"
            >
              This terminal will keep evolving day after day as new
              commands, features and ideas are added.
            </p>

            <div class="mt-7 flex flex-wrap gap-2">

              <span
                class="rounded-full border border-[var(--white-icon-tr)]
                px-3 py-1 text-xs font-mono
                text-[var(--white-icon)]"
              >
                Astro
              </span>

              <span
                class="rounded-full border border-[var(--white-icon-tr)]
                px-3 py-1 text-xs font-mono
                text-[var(--white-icon)]"
              >
                TypeScript
              </span>

              <span
                class="rounded-full border border-[var(--white-icon-tr)]
                px-3 py-1 text-xs font-mono
                text-[var(--white-icon)]"
              >
                Interactive UI
              </span>

            </div>

          </div>

          <div
            class="flex md:flex-col items-center md:items-end
            justify-between gap-5 md:min-w-[150px]"
          >

            <div
              class="font-mono text-xs
              text-[var(--white-icon)] opacity-60"
            >
              01 / 07
            </div>

            <div
              class="flex items-center gap-2
              text-sm text-[var(--sec)]
              transition-transform duration-300
              group-hover:translate-x-1"
            >
              <span>Open terminal</span>
              <span class="text-lg">→</span>
            </div>

          </div>

        </div>

      </a>
    </div>


    <!-- ========================================================= -->
    <!-- PROJECT 02 — MOVIE RANKING -->
    <!-- ========================================================= -->

    <div
      class="group relative mb-10 overflow-hidden rounded-2xl
      border border-[var(--white-icon-tr)]
      bg-[#1414149c] backdrop-blur-sm
      transition-all duration-500
      hover:border-[var(--sec)]/40
      hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
    >

      <a
        href="/projects/movie-ranking"
        class="relative block p-7 md:p-10"
      >

        <div class="flex flex-col md:flex-row gap-8">

          <div class="flex flex-1 flex-col justify-between">

            <div>

              <div class="mb-5 flex items-center gap-3">

                <span
                  class="font-mono text-xs tracking-[0.2em]
                  text-[var(--sec)]"
                >
                  PROJECT 02
                </span>

                <span
                  class="h-px w-12 bg-[var(--sec)] opacity-40"
                ></span>

                <span
                  class="text-xs uppercase tracking-widest
                  text-[var(--white-icon)]"
                >
                  Web App
                </span>

              </div>

              <h3
                class="text-2xl md:text-4xl font-medium
                tracking-tight text-[var(--white)]
                transition-colors duration-300
                group-hover:text-[var(--sec)]"
              >
                Movie Ranking
                <span class="text-[var(--sec)]">.</span>
              </h3>

              <p
                class="mt-4 max-w-2xl text-base md:text-lg
                leading-relaxed text-[var(--white-icon)]"
              >
                A movie ranking interface where films can be explored
                through their covers, ratings and personal reviews.
              </p>

              <div
                class="mt-7 flex flex-wrap items-center gap-x-6 gap-y-4"
              >

                <div>
                  <p
                    class="text-xs uppercase tracking-widest
                    text-[var(--white-icon)] opacity-60"
                  >
                    Featured movie
                  </p>

                  <p
                    class="mt-1 text-lg font-medium
                    text-[var(--white)]"
                  >
                    Avengers: Infinity War
                  </p>
                </div>

                <div
                  class="h-8 w-px bg-[var(--white-icon-tr)]
                  hidden sm:block"
                ></div>

                <div>
                  <p
                    class="text-xs uppercase tracking-widest
                    text-[var(--white-icon)] opacity-60"
                  >
                    Rating
                  </p>

                  <p
                    class="mt-1 font-mono text-lg
                    text-[var(--sec)]"
                  >
                    9<span class="text-[var(--white-icon)]">/10</span>
                  </p>
                </div>

              </div>

            </div>

            <div
              class="mt-8 flex flex-col sm:flex-row
              sm:items-center sm:justify-between gap-5"
            >

              <div class="flex flex-wrap gap-2">

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Astro
                </span>

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  UI
                </span>

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Movies
                </span>

              </div>

              <div
                class="flex items-center gap-2
                text-sm text-[var(--sec)]
                transition-transform duration-300
                group-hover:translate-x-1"
              >
                <span>View project</span>
                <span class="text-lg">→</span>
              </div>

            </div>

          </div>

        </div>

      </a>
    </div>


    <!-- ========================================================= -->
    <!-- PROJECT 03 — WEATHER -->
    <!-- ========================================================= -->

    <div
      class="group relative mb-10 overflow-hidden rounded-2xl
      border border-[var(--white-icon-tr)]
      bg-[#1414149c] backdrop-blur-sm
      transition-all duration-500
      hover:border-[var(--sec)]/40
      hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
    >

      <div
        class="pointer-events-none absolute inset-0 overflow-hidden opacity-60"
      >

        <div
          class="absolute -right-32 -top-32
          h-72 w-72 rounded-full
          bg-[var(--sec)] opacity-[0.06]
          blur-3xl
          transition-all duration-700
          group-hover:opacity-[0.12]
          group-hover:scale-110"
        ></div>

        <div
          class="absolute -left-32 -bottom-40
          h-80 w-80 rounded-full
          bg-[#0f5132] opacity-[0.05]
          blur-3xl
          transition-all duration-700
          group-hover:opacity-[0.09]"
        ></div>

        <div
          class="absolute inset-0 opacity-[0.12]"
          style="
            background-image:
              linear-gradient(rgba(255,255,255,0.04) 1px, transparent 1px),
              linear-gradient(90deg, rgba(255,255,255,0.04) 1px, transparent 1px);
            background-size: 42px 42px;
            mask-image: radial-gradient(circle at center, black, transparent 75%);
            -webkit-mask-image: radial-gradient(circle at center, black, transparent 75%);
          "
        ></div>

      </div>

      <a
        href="/projects/weather"
        class="relative block p-7 md:p-10"
      >

        <div class="flex flex-col md:flex-row gap-8">

          <div class="flex flex-1 flex-col justify-between">

            <div>

              <div class="mb-5 flex items-center gap-3">

                <span
                  class="font-mono text-xs tracking-[0.2em]
                  text-[var(--sec)]"
                >
                  PROJECT 03
                </span>

                <span
                  class="h-px w-12 bg-[var(--sec)] opacity-40"
                ></span>

                <span
                  class="text-xs uppercase tracking-widest
                  text-[var(--white-icon)]"
                >
                  Live Data
                </span>

              </div>

              <h3
                class="text-2xl md:text-4xl font-medium
                tracking-tight text-[var(--white)]
                transition-colors duration-300
                group-hover:text-[var(--sec)]"
              >
                Weather Dashboard
                <span class="text-[var(--sec)]">.</span>
              </h3>

              <p
                class="mt-4 max-w-2xl text-base md:text-lg
                leading-relaxed text-[var(--white-icon)]"
              >
                A dynamic weather dashboard displaying live weather
                conditions for Alcoi and Valencia through a clean
                developer-focused interface.
              </p>

              <div
                class="mt-7 grid grid-cols-1 sm:grid-cols-2 gap-3 max-w-2xl"
              >

                <div
                  class="relative overflow-hidden rounded-xl
                  border border-[var(--white-icon-tr)]
                  bg-[#10101080] px-5 py-4
                  backdrop-blur-md"
                >

                  <div class="flex items-center justify-between gap-4">

                    <div>

                      <p
                        class="text-xs uppercase tracking-widest
                        text-[var(--white-icon)] opacity-60"
                      >
                        Location
                      </p>

                      <p
                        class="mt-1 text-lg font-medium
                        text-[var(--white)]"
                      >
                        Alcoi
                      </p>

                    </div>

                    <div class="text-right">

                      <p
                        id="weather-alcoi-temperature"
                        class="font-mono text-xl
                        text-[var(--sec)]"
                      >
                        --°C
                      </p>

                      <p
                        id="weather-alcoi-status"
                        class="mt-1 text-xs
                        text-[var(--white-icon)]"
                      >
                        Loading...
                      </p>

                    </div>

                  </div>

                </div>

                <div
                  class="relative overflow-hidden rounded-xl
                  border border-[var(--white-icon-tr)]
                  bg-[#10101080] px-5 py-4
                  backdrop-blur-md"
                >

                  <div class="flex items-center justify-between gap-4">

                    <div>

                      <p
                        class="text-xs uppercase tracking-widest
                        text-[var(--white-icon)] opacity-60"
                      >
                        Location
                      </p>

                      <p
                        class="mt-1 text-lg font-medium
                        text-[var(--white)]"
                      >
                        Valencia
                      </p>

                    </div>

                    <div class="text-right">

                      <p
                        id="weather-valencia-temperature"
                        class="font-mono text-xl
                        text-[var(--sec)]"
                      >
                        --°C
                      </p>

                      <p
                        id="weather-valencia-status"
                        class="mt-1 text-xs
                        text-[var(--white-icon)]"
                      >
                        Loading...
                      </p>

                    </div>

                  </div>

                </div>

              </div>

            </div>

            <div
              class="mt-8 flex flex-col sm:flex-row
              sm:items-center sm:justify-between gap-5"
            >

              <div class="flex flex-wrap gap-2">

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Astro
                </span>

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  API
                </span>

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Live Weather
                </span>

              </div>

              <div
                class="flex items-center gap-2
                text-sm text-[var(--sec)]
                transition-transform duration-300
                group-hover:translate-x-1"
              >
                <span>View weather</span>
                <span class="text-lg">→</span>
              </div>

            </div>

          </div>

        </div>

      </a>
    </div>


    <!-- ========================================================= -->
    <!-- PROJECT 04 — ALGORITHM VISUALIZER -->
    <!-- ========================================================= -->

    <div
      class="group relative mb-10 overflow-hidden rounded-2xl
      border border-[var(--white-icon-tr)]
      bg-[#1414149c] backdrop-blur-sm
      transition-all duration-500
      hover:border-[var(--sec)]/40
      hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
    >

      <div
        class="pointer-events-none absolute -right-24 -bottom-32
        h-72 w-72 rounded-full
        bg-[var(--sec)] opacity-[0.045] blur-3xl
        transition-opacity duration-500
        group-hover:opacity-[0.09]"
      ></div>

      <a
        href="/projects/visualizer"
        class="relative block p-7 md:p-10"
      >

        <div class="flex flex-col md:flex-row gap-8">

          <div class="flex flex-1 flex-col justify-between">

            <div>

              <div class="mb-5 flex items-center gap-3">

                <span
                  class="font-mono text-xs tracking-[0.2em]
                  text-[var(--sec)]"
                >
                  PROJECT 04
                </span>

                <span
                  class="h-px w-12 bg-[var(--sec)] opacity-40"
                ></span>

                <span
                  class="text-xs uppercase tracking-widest
                  text-[var(--white-icon)]"
                >
                  CS Fundamentals
                </span>

              </div>

              <h3
                class="text-2xl md:text-4xl font-medium
                tracking-tight text-[var(--white)]
                transition-colors duration-300
                group-hover:text-[var(--sec)]"
              >
                Algorithm Visualizer
                <span class="text-[var(--sec)]">.</span>
              </h3>

              <p
                class="mt-4 max-w-2xl text-base md:text-lg
                leading-relaxed text-[var(--white-icon)]"
              >
                An interactive playground for watching data structures
                think: sorting algorithms step through comparisons and
                swaps, a hash table resolves collisions with live
                chaining, and a grid searches for the shortest path
                with Dijkstra and A*.
              </p>

              <div
                class="mt-7 flex flex-wrap items-center gap-x-6 gap-y-4"
              >

                <div>

                  <p
                    class="text-xs uppercase tracking-widest
                    text-[var(--white-icon)] opacity-60"
                  >
                    Covers
                  </p>

                  <p
                    class="mt-1 text-lg font-medium
                    text-[var(--white)]"
                  >
                    Sorting &middot; Hashing &middot; Pathfinding
                  </p>

                </div>

              </div>

            </div>

            <div
              class="mt-8 flex flex-col sm:flex-row
              sm:items-center sm:justify-between gap-5"
            >

              <div class="flex flex-wrap gap-2">

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Astro
                </span>

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  TypeScript
                </span>

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Data Structures &amp; Algorithms
                </span>

              </div>

              <div
                class="flex items-center gap-2
                text-sm text-[var(--sec)]
                transition-transform duration-300
                group-hover:translate-x-1"
              >
                <span>Open visualizer</span>
                <span class="text-lg">→</span>
              </div>

            </div>

          </div>

        </div>

      </a>
    </div>


    <!-- ========================================================= -->
    <!-- PROJECT 05 — NETWORK PROTOCOL SIMULATOR -->
    <!-- ========================================================= -->

    <div
      class="group relative mt-10 overflow-hidden rounded-2xl
      border border-[var(--white-icon-tr)]
      bg-[#1414149c] backdrop-blur-sm
      transition-all duration-500
      hover:border-[var(--sec)]/40
      hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
    >

      <div
        class="pointer-events-none absolute -left-24 -top-24
        h-72 w-72 rounded-full
        bg-[var(--sec)] opacity-[0.045] blur-3xl
        transition-opacity duration-500
        group-hover:opacity-[0.09]"
      ></div>

      <a
        href="/projects/network"
        class="relative block p-7 md:p-10"
      >

        <div class="flex flex-col md:flex-row gap-8">

          <div class="flex flex-1 flex-col justify-between">

            <div>

              <div class="mb-5 flex items-center gap-3">

                <span
                  class="font-mono text-xs tracking-[0.2em]
                  text-[var(--sec)]"
                >
                  PROJECT 05
                </span>

                <span
                  class="h-px w-12 bg-[var(--sec)] opacity-40"
                ></span>

                <span
                  class="text-xs uppercase tracking-widest
                  text-[var(--white-icon)]"
                >
                  Networking
                </span>

              </div>

              <h3
                class="text-2xl md:text-4xl font-medium
                tracking-tight text-[var(--white)]
                transition-colors duration-300
                group-hover:text-[var(--sec)]"
              >
                Network Protocol Simulator
                <span class="text-[var(--sec)]">.</span>
              </h3>

              <p
                class="mt-4 max-w-2xl text-base md:text-lg
                leading-relaxed text-[var(--white-icon)]"
              >
                An interactive simulator for the TCP three-way handshake,
                TCP vs UDP packet loss and retransmission, and message
                framing over a byte stream &mdash; the transport layer,
                animated and stepped through by hand.
              </p>

              <div
                class="mt-7 flex flex-wrap items-center gap-x-6 gap-y-4"
              >

                <div>

                  <p
                    class="text-xs uppercase tracking-widest
                    text-[var(--white-icon)] opacity-60"
                  >
                    Covers
                  </p>

                  <p
                    class="mt-1 text-lg font-medium
                    text-[var(--white)]"
                  >
                    TCP Handshake &middot; Packet Loss &middot; Framing
                  </p>

                </div>

              </div>

            </div>

            <div
              class="mt-8 flex flex-col sm:flex-row
              sm:items-center sm:justify-between gap-5"
            >

              <div class="flex flex-wrap gap-2">

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Astro
                </span>

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  TypeScript
                </span>

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  TCP/UDP
                </span>

              </div>

              <div
                class="flex items-center gap-2
                text-sm text-[var(--sec)]
                transition-transform duration-300
                group-hover:translate-x-1"
              >
                <span>Open simulator</span>
                <span class="text-lg">→</span>
              </div>

            </div>

          </div>

        </div>

      </a>
    </div>


    <!-- ========================================================= -->
    <!-- PROJECT 06 — CPU SIMULATOR -->
    <!-- ========================================================= -->

    <div
      class="group relative mt-10 overflow-hidden rounded-2xl
      border border-[var(--white-icon-tr)]
      bg-[#1414149c] backdrop-blur-sm
      transition-all duration-500
      hover:border-[var(--sec)]/40
      hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
    >

      <div
        class="pointer-events-none absolute -right-24 -bottom-24
        h-72 w-72 rounded-full
        bg-[var(--sec)] opacity-[0.045] blur-3xl
        transition-opacity duration-500
        group-hover:opacity-[0.09]"
      ></div>

      <a
        href="/projects/Cpu"
        class="relative block p-7 md:p-10"
      >

        <div class="flex flex-col md:flex-row gap-8">

          <div class="flex flex-1 flex-col justify-between">

            <div>

              <div class="mb-5 flex items-center gap-3">

                <span
                  class="font-mono text-xs tracking-[0.2em]
                  text-[var(--sec)]"
                >
                  PROJECT 06
                </span>

                <span
                  class="h-px w-12 bg-[var(--sec)] opacity-40"
                ></span>

                <span
                  class="text-xs uppercase tracking-widest
                  text-[var(--white-icon)]"
                >
                  Computer Architecture
                </span>

              </div>

              <h3
                class="text-2xl md:text-4xl font-medium
                tracking-tight text-[var(--white)]
                transition-colors duration-300
                group-hover:text-[var(--sec)]"
              >
                CPU Simulator
                <span class="text-[var(--sec)]">.</span>
              </h3>

              <p
                class="mt-4 max-w-2xl text-base md:text-lg
                leading-relaxed text-[var(--white-icon)]"
              >
                A tiny 8-register CPU with its own instruction set,
                stepping through fetch, decode and execute one
                instruction at a time &mdash; write a short program
                and watch the registers and memory change live.
              </p>

              <div
                class="mt-7 flex flex-wrap items-center gap-x-6 gap-y-4"
              >

                <div>

                  <p
                    class="text-xs uppercase tracking-widest
                    text-[var(--white-icon)] opacity-60"
                  >
                    Covers
                  </p>

                  <p
                    class="mt-1 text-lg font-medium
                    text-[var(--white)]"
                  >
                    Fetch-Decode-Execute &middot; Registers &middot; Memory
                  </p>

                </div>

              </div>

            </div>

            <div
              class="mt-8 flex flex-col sm:flex-row
              sm:items-center sm:justify-between gap-5"
            >

              <div class="flex flex-wrap gap-2">

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Astro
                </span>

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  TypeScript
                </span>

                <span
                  class="rounded-full border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Assembly
                </span>

              </div>

              <div
                class="flex items-center gap-2
                text-sm text-[var(--sec)]
                transition-transform duration-300
                group-hover:translate-x-1"
              >
                <span>Open simulator</span>
                <span class="text-lg">→</span>
              </div>

            </div>

          </div>

        </div>

      </a>
    </div>


    <!-- ========================================================= -->
    <!-- PROJECT 07 — EXPENSE SPLITTER / FLUTTER APP -->
    <!-- ========================================================= -->

    <div
      class="group relative mt-10 overflow-hidden rounded-2xl
      border border-[var(--white-icon-tr)]
      bg-[#1414149c] backdrop-blur-sm
      transition-all duration-500
      hover:border-[var(--sec)]/40
      hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
    >

      <div
        class="pointer-events-none absolute -left-24 -top-24
        h-72 w-72 rounded-full
        bg-[var(--sec)] opacity-[0.045] blur-3xl
        transition-opacity duration-500
        group-hover:opacity-[0.09]"
      ></div>

      <!--
        Expense Splitter is a Flutter web build, deployed as static
        files at public/projects/expense-splitter/ in this repo.
        Build it with:
          flutter build web --release --base-href /projects/expense-splitter/
        then copy the contents of build/web/ into
        public/projects/expense-splitter/ so this href resolves.
      -->

      <a
        href="/projects/expense-splitter/"
        class="relative block p-7 md:p-10"
      >

        <div class="flex flex-col md:flex-row gap-8">

          <div class="flex flex-1 flex-col justify-between">

            <div>

              <div class="mb-5 flex items-center gap-3">

                <span
                  class="font-mono text-xs tracking-[0.2em]
                  text-[var(--sec)]"
                >
                  PROJECT 07
                </span>

                <span
                  class="h-px w-12 bg-[var(--sec)] opacity-40"
                ></span>

                <span
                  class="text-xs uppercase tracking-widest
                  text-[var(--white-icon)]"
                >
                  Flutter App
                </span>

              </div>

              <h3
                class="text-2xl md:text-4xl font-medium
                tracking-tight text-[var(--white)]
                transition-colors duration-300
                group-hover:text-[var(--sec)]"
              >
                Expense Splitter
                <span class="text-[var(--sec)]">.</span>
              </h3>

              <p
                class="mt-4 max-w-2xl text-base md:text-lg
                leading-relaxed text-[var(--white-icon)]"
              >
                A Flutter mobile application for splitting group expenses,
                calculating balances between participants and minimizing
                the number of transfers needed to settle shared costs.
              </p>

              <div
                class="mt-7 flex flex-wrap items-center gap-x-6 gap-y-4"
              >

                <div>

                  <p
                    class="text-xs uppercase tracking-widest
                    text-[var(--white-icon)] opacity-60"
                  >
                    Platform
                  </p>

                  <p
                    class="mt-1 text-lg font-medium
                    text-[var(--white)]"
                  >
                    Android &middot; iOS &middot; Web
                  </p>

                </div>

                <div
                  class="hidden sm:block h-8 w-px
                  bg-[var(--white-icon-tr)]"
                ></div>

                <div>

                  <p
                    class="text-xs uppercase tracking-widest
                    text-[var(--white-icon)] opacity-60"
                  >
                    Technology
                  </p>

                  <p
                    class="mt-1 text-lg font-medium
                    text-[var(--white)]"
                  >
                    Flutter &middot; Dart
                  </p>

                </div>

              </div>

            </div>

            <div
              class="mt-8 flex flex-col sm:flex-row
              sm:items-center sm:justify-between gap-5"
            >

              <div class="flex flex-wrap gap-2">

                <span
                  class="rounded-full
                  border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Flutter
                </span>

                <span
                  class="rounded-full
                  border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Dart
                </span>

                <span
                  class="rounded-full
                  border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Mobile
                </span>

                <span
                  class="rounded-full
                  border border-[var(--white-icon-tr)]
                  px-3 py-1 text-xs font-mono
                  text-[var(--white-icon)]"
                >
                  Expense Management
                </span>

              </div>

              <div
                class="flex items-center gap-2
                text-sm text-[var(--sec)]
                transition-transform duration-300
                group-hover:translate-x-1"
              >
                <span>Open Flutter app</span>

                <span class="text-lg">
                  →
                </span>
              </div>

            </div>

          </div>

        </div>

      </a>
    </div>

  </div>
</section>

<script>
  interface WeatherData {
    current: {
      temperature_2m: number;
      weather_code: number;
    };
  }

  interface WeatherLocation {
    latitude: number;
    longitude: number;
    temperatureElement: string;
    statusElement: string;
  }

  const weatherLocations: WeatherLocation[] = [
    {
      latitude: 38.6983,
      longitude: -0.4743,
      temperatureElement: "weather-alcoi-temperature",
      statusElement: "weather-alcoi-status",
    },
    {
      latitude: 39.4699,
      longitude: -0.3763,
      temperatureElement: "weather-valencia-temperature",
      statusElement: "weather-valencia-status",
    },
  ];

  function getWeatherDescription(code: number): string {
    if (code === 0) return "Clear";
    if (code === 1) return "Mainly clear";
    if (code === 2) return "Partly cloudy";
    if (code === 3) return "Cloudy";
    if (code === 45 || code === 48) return "Fog";
    if (code >= 51 && code <= 57) return "Drizzle";
    if (code >= 61 && code <= 67) return "Rain";
    if (code >= 71 && code <= 77) return "Snow";
    if (code >= 80 && code <= 82) return "Showers";
    if (code >= 85 && code <= 86) return "Snow showers";
    if (code >= 95 && code <= 99) return "Thunderstorm";

    return "Unknown";
  }

  async function loadWeather(location: WeatherLocation) {
    const temperatureElement = document.getElementById(
      location.temperatureElement
    );

    const statusElement = document.getElementById(
      location.statusElement
    );

    if (!temperatureElement || !statusElement) return;

    try {
      const url =
        `https://api.open-meteo.com/v1/forecast` +
        `?latitude=${location.latitude}` +
        `&longitude=${location.longitude}` +
        `&current=temperature_2m,weather_code` +
        `&timezone=Europe%2FMadrid`;

      const response = await fetch(url);

      if (!response.ok) {
        throw new Error("Weather request failed");
      }

      const data = (await response.json()) as WeatherData;

      const temperature = data.current.temperature_2m;
      const weatherCode = data.current.weather_code;

      temperatureElement.textContent =
        `${Math.round(temperature)}°C`;

      statusElement.textContent =
        getWeatherDescription(weatherCode);

    } catch {
      temperatureElement.textContent = "--°C";
      statusElement.textContent = "Unavailable";
    }
  }

  function loadAllWeather() {
    weatherLocations.forEach((location) => {
      void loadWeather(location);
    });
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", loadAllWeather);
  } else {
    loadAllWeather();
  }
</script>

<style is:global>

  /* ============================================================= */
  /* BACKGROUND — MOMIJI RED / FOREST GREEN AURORA (Projects only) */
  /* ============================================================= */
  /*
    Self-contained variant of the nebula background from home.astro.
    Uses -alt / -Alt suffixed class + keyframe names so it never
    collides with the blue version used in the hero section, even
    if both files share a global stylesheet.
  */

  .hero-dots-alt {
    background-image:
      radial-gradient(
        rgba(255, 255, 255, 0.14) 1px,
        transparent 1.4px
      );

    background-size: 26px 26px;

    mask-image:
      radial-gradient(
        ellipse 85% 75% at 42% 38%,
        black 0%,
        rgba(0, 0, 0, 0.5) 55%,
        transparent 85%
      );

    -webkit-mask-image:
      radial-gradient(
        ellipse 85% 75% at 42% 38%,
        black 0%,
        rgba(0, 0, 0, 0.5) 55%,
        transparent 85%
      );
  }

  .noise-layer-alt {
    background-image:
      url("data:image/svg+xml,%3Csvg viewBox='0 0 180 180' xmlns='http://www.w3.org/2000/svg'%3E%3Cfilter id='n'%3E%3CfeTurbulence type='fractalNoise' baseFrequency='.9' numOctaves='4' stitchTiles='stitch'/%3E%3C/filter%3E%3Crect width='100%25' height='100%25' filter='url(%23n)' opacity='.4'/%3E%3C/svg%3E");
  }

  .hero-vignette-alt {
    position: absolute;
    inset: 0;

    background:
      linear-gradient(
        90deg,
        rgba(5, 6, 12, 0.55) 0%,
        transparent 26%,
        transparent 72%,
        rgba(5, 6, 12, 0.55) 100%
      ),
      radial-gradient(
        ellipse 72% 72% at 50% 45%,
        transparent 30%,
        rgba(5, 6, 12, 0.55) 100%
      );
  }

  .nebula-field-alt {
    position: absolute;
    inset: -15%;

    transform: rotate(-28deg);

    transform-origin: 30% 55%;
  }

  /* Wide, soft outer wash — mostly deep forest green, torii-red only at the right edge */
  .ribbon-back-alt {
    position: absolute;

    left: -10%;
    top: 38%;

    width: 135%;
    height: 46%;

    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(20, 83, 45, 0.34) 18%,
      rgba(45, 106, 79, 0.36) 38%,
      rgba(64, 145, 108, 0.24) 58%,
      rgba(217, 51, 63, 0.16) 82%,
      transparent 100%
    );

    filter: blur(70px);

    animation: nebulaBreatheAlt 16s ease-in-out infinite;
  }

  /* Mid-density body — pine/moss green dominates, torii vermillion only bleeds in on the right */
  .ribbon-mid-alt {
    position: absolute;

    left: -5%;
    top: 44%;

    width: 120%;
    height: 26%;

    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(20, 83, 45, 0.4) 20%,
      rgba(45, 106, 79, 0.52) 42%,
      rgba(64, 145, 108, 0.3) 60%,
      rgba(217, 51, 63, 0.28) 84%,
      transparent 96%
    );

    filter: blur(42px);

    mix-blend-mode: screen;

    animation: nebulaBreatheAlt 16s ease-in-out infinite reverse;
  }

  /* Bright, thin core streak — pale moss highlight fading to a torii-red edge on the right */
  .ribbon-core-alt {
    position: absolute;

    left: 2%;
    top: 49.5%;

    width: 96%;
    height: 7%;

    border-radius: 999px;

    background: linear-gradient(
      90deg,
      transparent 0%,
      rgba(190, 242, 214, 0.1) 16%,
      rgba(224, 250, 235, 0.55) 42%,
      rgba(64, 145, 108, 0.4) 62%,
      rgba(217, 51, 63, 0.32) 86%,
      transparent 96%
    );

    filter: blur(14px);

    mix-blend-mode: screen;

    opacity: 0.85;

    animation: nebulaShimmerAlt 9s ease-in-out infinite;
  }

  .nebula-glow-alt {
    position: absolute;

    border-radius: 999px;

    filter: blur(90px);

    mix-blend-mode: screen;
  }

  /* Forest green ambient glow — left side, large and dominant */
  .glow-a-alt {
    left: 2%;
    top: 0%;

    width: 52vw;
    height: 52vw;

    max-width: 680px;
    max-height: 680px;

    background: radial-gradient(circle, rgba(20, 83, 45, 0.6) 0%, rgba(45, 106, 79, 0.28) 45%, transparent 72%);

    animation: driftOneAlt 24s ease-in-out infinite;
  }

  /* Torii-gate vermillion ambient glow — right side, smaller accent */
  .glow-b-alt {
    right: 4%;
    bottom: 6%;

    width: 24vw;
    height: 24vw;

    max-width: 340px;
    max-height: 340px;

    background: radial-gradient(circle, rgba(217, 51, 63, 0.34) 0%, transparent 72%);

    animation: driftTwoAlt 28s ease-in-out infinite;
  }

  @keyframes nebulaBreatheAlt {
    0%, 100% { transform: scaleX(1) translateX(0); opacity: 1; }
    50% { transform: scaleX(1.06) translateX(2%); opacity: 0.85; }
  }

  @keyframes nebulaShimmerAlt {
    0%, 100% { opacity: 0.7; filter: blur(14px); }
    50% { opacity: 1; filter: blur(10px); }
  }

  @keyframes driftOneAlt {
    0%, 100% { transform: translate3d(0, 0, 0) scale(1); }
    50% { transform: translate3d(4%, 6%, 0) scale(1.08); }
  }

  @keyframes driftTwoAlt {
    0%, 100% { transform: translate3d(0, 0, 0) scale(1); }
    50% { transform: translate3d(-6%, 4%, 0) scale(0.94); }
  }

  @media (max-width: 640px) {

    .nebula-field-alt {
      inset: -25%;
    }

    .ribbon-back-alt,
    .ribbon-mid-alt,
    .ribbon-core-alt {
      filter: blur(38px);
    }

    .glow-a-alt,
    .glow-b-alt {
      width: 65vw;
      height: 65vw;

      filter: blur(60px);
    }

    .hero-dots-alt {
      background-size: 20px 20px;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .ribbon-back-alt,
    .ribbon-mid-alt,
    .ribbon-core-alt,
    .nebula-glow-alt {
      animation: none;
    }
  }

</style>