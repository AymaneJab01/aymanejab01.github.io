---

---

<section
  id="projects"
  class="text-[var(--white)] py-24 md:py-36"
>
  <div class="max-w-6xl mx-auto px-6">

```
<!-- ========================================================= -->
<!-- SECTION HEADER -->
<!-- ========================================================= -->

<div class="mb-16 md:mb-20">
  <div class="flex items-center gap-3 mb-5">
    <span
      class="h-2 w-2 rounded-full bg-[var(--sec)]
      shadow-[0_0_12px_var(--sec)]"
    ></span>

    <span
      class="text-sm tracking-[0.25em] uppercase
      text-[var(--sec)] shiny-sec"
    >
      Projects
    </span>
  </div>

  <h2
    class="text-4xl md:text-6xl font-medium tracking-tight
    text-[var(--white)]"
  >
    Things I've built<span class="text-[var(--sec)]">.</span>
  </h2>

  <p
    class="mt-5 max-w-2xl text-lg
    text-[var(--white-icon)] leading-relaxed"
  >
    A collection of projects, experiments and ideas that
    represent what I'm building and learning as a software developer.
  </p>
</div>


<!-- ========================================================= -->
<!-- PROJECT 01 — TERMINAL -->
<!-- ========================================================= -->

<div
  class="group relative mb-10 overflow-hidden rounded-[28px]
  border border-white/[0.12]
  bg-white/[0.045] backdrop-blur-2xl
  backdrop-saturate-150
  transition-all duration-500
  hover:border-[var(--sec)]/40
  hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
>

  <div
    class="pointer-events-none absolute -right-32 -top-32
    h-64 w-64 rounded-full
    bg-[var(--sec)] opacity-[0.04] blur-3xl
    transition-opacity duration-500
    group-hover:opacity-[0.08]"
  ></div>

  <a
    href="/#terminal"
    class="relative block p-7 md:p-10"
  >

    <div class="flex flex-col md:flex-row md:items-start md:justify-between gap-8">

      <div class="flex-1">

        <div class="mb-5 flex items-center gap-3">
          <span
            class="font-mono text-xs tracking-[0.2em]
            text-[var(--sec)]"
          >
            PROJECT 01
          </span>

          <span
            class="h-px w-12 bg-[var(--sec)] opacity-40"
          ></span>

          <span
            class="text-xs uppercase tracking-widest
            text-[var(--white-icon)]"
          >
            Interactive
          </span>
        </div>

        <h3
          class="text-2xl md:text-4xl font-medium
          tracking-tight text-[var(--white)]
          transition-colors duration-300
          group-hover:text-[var(--sec)]"
        >
          Developer Terminal
          <span class="text-[var(--sec)]">.</span>
        </h3>

        <p
          class="mt-4 max-w-2xl text-base md:text-lg
          leading-relaxed text-[var(--white-icon)]"
        >
          An interactive terminal built directly into my portfolio,
          designed to provide a developer-style way of exploring
          my work, skills and information.
        </p>

        <p
          class="mt-3 max-w-2xl text-sm
          leading-relaxed text-[var(--white-icon)]
          opacity-70"
        >
          This terminal will keep evolving day after day as new
          commands, features and ideas are added.
        </p>

        <div class="mt-7 flex flex-wrap gap-2">

          <span
            class="rounded-full border border-[var(--white-icon-tr)]
            px-3 py-1 text-xs font-mono
            text-[var(--white-icon)]"
          >
            Astro
          </span>

          <span
            class="rounded-full border border-[var(--white-icon-tr)]
            px-3 py-1 text-xs font-mono
            text-[var(--white-icon)]"
          >
            TypeScript
          </span>

          <span
            class="rounded-full border border-[var(--white-icon-tr)]
            px-3 py-1 text-xs font-mono
            text-[var(--white-icon)]"
          >
            Interactive UI
          </span>

        </div>

      </div>

      <div
        class="flex md:flex-col items-center md:items-end
        justify-between gap-5 md:min-w-[150px]"
      >

        <div
          class="font-mono text-xs
          text-[var(--white-icon)] opacity-60"
        >
          01 / 07
        </div>

        <div
          class="flex items-center gap-2
          text-sm text-[var(--sec)]
          transition-transform duration-300
          group-hover:translate-x-1"
        >
          <span>Open terminal</span>
          <span class="text-lg">→</span>
        </div>

      </div>

    </div>

  </a>
</div>


<!-- ========================================================= -->
<!-- PROJECT 02 — MOVIE RANKING -->
<!-- ========================================================= -->

<div
  class="group relative mb-10 overflow-hidden rounded-[28px]
  border border-white/[0.12]
  bg-white/[0.045] backdrop-blur-2xl
  backdrop-saturate-150
  transition-all duration-500
  hover:border-[var(--sec)]/40
  hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
>

  <a
    href="/projects/movie-ranking"
    class="relative block p-7 md:p-10"
  >

    <div class="flex flex-col md:flex-row gap-8">

      <div class="flex flex-1 flex-col justify-between">

        <div>

          <div class="mb-5 flex items-center gap-3">

            <span
              class="font-mono text-xs tracking-[0.2em]
              text-[var(--sec)]"
            >
              PROJECT 02
            </span>

            <span
              class="h-px w-12 bg-[var(--sec)] opacity-40"
            ></span>

            <span
              class="text-xs uppercase tracking-widest
              text-[var(--white-icon)]"
            >
              Web App
            </span>

          </div>

          <h3
            class="text-2xl md:text-4xl font-medium
            tracking-tight text-[var(--white)]
            transition-colors duration-300
            group-hover:text-[var(--sec)]"
          >
            Movie Ranking
            <span class="text-[var(--sec)]">.</span>
          </h3>

          <p
            class="mt-4 max-w-2xl text-base md:text-lg
            leading-relaxed text-[var(--white-icon)]"
          >
            A movie ranking interface where films can be explored
            through their covers, ratings and personal reviews.
          </p>

          <div
            class="mt-7 flex flex-wrap items-center gap-x-6 gap-y-4"
          >

            <div>
              <p
                class="text-xs uppercase tracking-widest
                text-[var(--white-icon)] opacity-60"
              >
                Featured movie
              </p>

              <p
                class="mt-1 text-lg font-medium
                text-[var(--white)]"
              >
                Avengers: Infinity War
              </p>
            </div>

            <div
              class="h-8 w-px bg-[var(--white-icon-tr)]
              hidden sm:block"
            ></div>

            <div>
              <p
                class="text-xs uppercase tracking-widest
                text-[var(--white-icon)] opacity-60"
              >
                Rating
              </p>

              <p
                class="mt-1 font-mono text-lg
                text-[var(--sec)]"
              >
                9<span class="text-[var(--white-icon)]">/10</span>
              </p>
            </div>

          </div>

        </div>

        <div
          class="mt-8 flex flex-col sm:flex-row
          sm:items-center sm:justify-between gap-5"
        >

          <div class="flex flex-wrap gap-2">

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Astro
            </span>

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              UI
            </span>

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Movies
            </span>

          </div>

          <div
            class="flex items-center gap-2
            text-sm text-[var(--sec)]
            transition-transform duration-300
            group-hover:translate-x-1"
          >
            <span>View project</span>
            <span class="text-lg">→</span>
          </div>

        </div>

      </div>

    </div>

  </a>
</div>


<!-- ========================================================= -->
<!-- PROJECT 03 — WEATHER -->
<!-- ========================================================= -->

<div
  class="group relative mb-10 overflow-hidden rounded-[28px]
  border border-white/[0.12]
  bg-white/[0.045] backdrop-blur-2xl
  backdrop-saturate-150
  transition-all duration-500
  hover:border-[var(--sec)]/40
  hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
>

  <div
    class="pointer-events-none absolute inset-0 overflow-hidden opacity-60"
  >

    <div
      class="absolute -right-32 -top-32
      h-72 w-72 rounded-full
      bg-[var(--sec)] opacity-[0.06]
      blur-3xl
      transition-all duration-700
      group-hover:opacity-[0.12]
      group-hover:scale-110"
    ></div>

    <div
      class="absolute -left-32 -bottom-40
      h-80 w-80 rounded-full
      bg-[#0f5132] opacity-[0.05]
      blur-3xl
      transition-all duration-700
      group-hover:opacity-[0.09]"
    ></div>

    <div
      class="absolute inset-0 opacity-[0.12]"
      style="
        background-image:
          linear-gradient(rgba(255,255,255,0.04) 1px, transparent 1px),
          linear-gradient(90deg, rgba(255,255,255,0.04) 1px, transparent 1px);
        background-size: 42px 42px;
        mask-image: radial-gradient(circle at center, black, transparent 75%);
        -webkit-mask-image: radial-gradient(circle at center, black, transparent 75%);
      "
    ></div>

  </div>

  <a
    href="/projects/weather"
    class="relative block p-7 md:p-10"
  >

    <div class="flex flex-col md:flex-row gap-8">

      <div class="flex flex-1 flex-col justify-between">

        <div>

          <div class="mb-5 flex items-center gap-3">

            <span
              class="font-mono text-xs tracking-[0.2em]
              text-[var(--sec)]"
            >
              PROJECT 03
            </span>

            <span
              class="h-px w-12 bg-[var(--sec)] opacity-40"
            ></span>

            <span
              class="text-xs uppercase tracking-widest
              text-[var(--white-icon)]"
            >
              Live Data
            </span>

          </div>

          <h3
            class="text-2xl md:text-4xl font-medium
            tracking-tight text-[var(--white)]
            transition-colors duration-300
            group-hover:text-[var(--sec)]"
          >
            Weather Dashboard
            <span class="text-[var(--sec)]">.</span>
          </h3>

          <p
            class="mt-4 max-w-2xl text-base md:text-lg
            leading-relaxed text-[var(--white-icon)]"
          >
            A dynamic weather dashboard displaying live weather
            conditions for Alcoi and Valencia through a clean
            developer-focused interface.
          </p>

          <div
            class="mt-7 grid grid-cols-1 sm:grid-cols-2 gap-3 max-w-2xl"
          >

            <div
              class="relative overflow-hidden rounded-xl
              border border-[var(--white-icon-tr)]
              bg-white/[0.045] px-5 py-4
              backdrop-blur-xl backdrop-saturate-150"
            >

              <div class="flex items-center justify-between gap-4">

                <div>

                  <p
                    class="text-xs uppercase tracking-widest
                    text-[var(--white-icon)] opacity-60"
                  >
                    Location
                  </p>

                  <p
                    class="mt-1 text-lg font-medium
                    text-[var(--white)]"
                  >
                    Alcoi
                  </p>

                </div>

                <div class="text-right">

                  <p
                    id="weather-alcoi-temperature"
                    class="font-mono text-xl
                    text-[var(--sec)]"
                  >
                    --°C
                  </p>

                  <p
                    id="weather-alcoi-status"
                    class="mt-1 text-xs
                    text-[var(--white-icon)]"
                  >
                    Loading...
                  </p>

                </div>

              </div>

            </div>

            <div
              class="relative overflow-hidden rounded-xl
              border border-[var(--white-icon-tr)]
              bg-white/[0.045] px-5 py-4
              backdrop-blur-xl backdrop-saturate-150"
            >

              <div class="flex items-center justify-between gap-4">

                <div>

                  <p
                    class="text-xs uppercase tracking-widest
                    text-[var(--white-icon)] opacity-60"
                  >
                    Location
                  </p>

                  <p
                    class="mt-1 text-lg font-medium
                    text-[var(--white)]"
                  >
                    Valencia
                  </p>

                </div>

                <div class="text-right">

                  <p
                    id="weather-valencia-temperature"
                    class="font-mono text-xl
                    text-[var(--sec)]"
                  >
                    --°C
                  </p>

                  <p
                    id="weather-valencia-status"
                    class="mt-1 text-xs
                    text-[var(--white-icon)]"
                  >
                    Loading...
                  </p>

                </div>

              </div>

            </div>

          </div>

        </div>

        <div
          class="mt-8 flex flex-col sm:flex-row
          sm:items-center sm:justify-between gap-5"
        >

          <div class="flex flex-wrap gap-2">

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Astro
            </span>

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              API
            </span>

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Live Weather
            </span>

          </div>

          <div
            class="flex items-center gap-2
            text-sm text-[var(--sec)]
            transition-transform duration-300
            group-hover:translate-x-1"
          >
            <span>View weather</span>
            <span class="text-lg">→</span>
          </div>

        </div>

      </div>

    </div>

  </a>
</div>


<!-- ========================================================= -->
<!-- PROJECT 04 — ALGORITHM VISUALIZER -->
<!-- ========================================================= -->

<div
  class="group relative mb-10 overflow-hidden rounded-[28px]
  border border-white/[0.12]
  bg-white/[0.045] backdrop-blur-2xl
  backdrop-saturate-150
  transition-all duration-500
  hover:border-[var(--sec)]/40
  hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
>

  <div
    class="pointer-events-none absolute -right-24 -bottom-32
    h-72 w-72 rounded-full
    bg-[var(--sec)] opacity-[0.045] blur-3xl
    transition-opacity duration-500
    group-hover:opacity-[0.09]"
  ></div>

  <a
    href="/projects/visualizer"
    class="relative block p-7 md:p-10"
  >

    <div class="flex flex-col md:flex-row gap-8">

      <div class="flex flex-1 flex-col justify-between">

        <div>

          <div class="mb-5 flex items-center gap-3">

            <span
              class="font-mono text-xs tracking-[0.2em]
              text-[var(--sec)]"
            >
              PROJECT 04
            </span>

            <span
              class="h-px w-12 bg-[var(--sec)] opacity-40"
            ></span>

            <span
              class="text-xs uppercase tracking-widest
              text-[var(--white-icon)]"
            >
              CS Fundamentals
            </span>

          </div>

          <h3
            class="text-2xl md:text-4xl font-medium
            tracking-tight text-[var(--white)]
            transition-colors duration-300
            group-hover:text-[var(--sec)]"
          >
            Algorithm Visualizer
            <span class="text-[var(--sec)]">.</span>
          </h3>

          <p
            class="mt-4 max-w-2xl text-base md:text-lg
            leading-relaxed text-[var(--white-icon)]"
          >
            An interactive playground for watching data structures
            think: sorting algorithms step through comparisons and
            swaps, a hash table resolves collisions with live
            chaining, and a grid searches for the shortest path
            with Dijkstra and A*.
          </p>

          <div
            class="mt-7 flex flex-wrap items-center gap-x-6 gap-y-4"
          >

            <div>

              <p
                class="text-xs uppercase tracking-widest
                text-[var(--white-icon)] opacity-60"
              >
                Covers
              </p>

              <p
                class="mt-1 text-lg font-medium
                text-[var(--white)]"
              >
                Sorting &middot; Hashing &middot; Pathfinding
              </p>

            </div>

          </div>

        </div>

        <div
          class="mt-8 flex flex-col sm:flex-row
          sm:items-center sm:justify-between gap-5"
        >

          <div class="flex flex-wrap gap-2">

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Astro
            </span>

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              TypeScript
            </span>

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Data Structures &amp; Algorithms
            </span>

          </div>

          <div
            class="flex items-center gap-2
            text-sm text-[var(--sec)]
            transition-transform duration-300
            group-hover:translate-x-1"
          >
            <span>Open visualizer</span>
            <span class="text-lg">→</span>
          </div>

        </div>

      </div>

    </div>

  </a>
</div>


<!-- ========================================================= -->
<!-- PROJECT 05 — NETWORK PROTOCOL SIMULATOR -->
<!-- ========================================================= -->

<div
  class="group relative mt-10 overflow-hidden rounded-[28px]
  border border-white/[0.12]
  bg-white/[0.045] backdrop-blur-2xl
  backdrop-saturate-150
  transition-all duration-500
  hover:border-[var(--sec)]/40
  hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
>

  <div
    class="pointer-events-none absolute -left-24 -top-24
    h-72 w-72 rounded-full
    bg-[var(--sec)] opacity-[0.045] blur-3xl
    transition-opacity duration-500
    group-hover:opacity-[0.09]"
  ></div>

  <a
    href="/projects/network"
    class="relative block p-7 md:p-10"
  >

    <div class="flex flex-col md:flex-row gap-8">

      <div class="flex flex-1 flex-col justify-between">

        <div>

          <div class="mb-5 flex items-center gap-3">

            <span
              class="font-mono text-xs tracking-[0.2em]
              text-[var(--sec)]"
            >
              PROJECT 05
            </span>

            <span
              class="h-px w-12 bg-[var(--sec)] opacity-40"
            ></span>

            <span
              class="text-xs uppercase tracking-widest
              text-[var(--white-icon)]"
            >
              Networking
            </span>

          </div>

          <h3
            class="text-2xl md:text-4xl font-medium
            tracking-tight text-[var(--white)]
            transition-colors duration-300
            group-hover:text-[var(--sec)]"
          >
            Network Protocol Simulator
            <span class="text-[var(--sec)]">.</span>
          </h3>

          <p
            class="mt-4 max-w-2xl text-base md:text-lg
            leading-relaxed text-[var(--white-icon)]"
          >
            An interactive simulator for the TCP three-way handshake,
            TCP vs UDP packet loss and retransmission, and message
            framing over a byte stream &mdash; the transport layer,
            animated and stepped through by hand.
          </p>

          <div
            class="mt-7 flex flex-wrap items-center gap-x-6 gap-y-4"
          >

            <div>

              <p
                class="text-xs uppercase tracking-widest
                text-[var(--white-icon)] opacity-60"
              >
                Covers
              </p>

              <p
                class="mt-1 text-lg font-medium
                text-[var(--white)]"
              >
                TCP Handshake &middot; Packet Loss &middot; Framing
              </p>

            </div>

          </div>

        </div>

        <div
          class="mt-8 flex flex-col sm:flex-row
          sm:items-center sm:justify-between gap-5"
        >

          <div class="flex flex-wrap gap-2">

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Astro
            </span>

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              TypeScript
            </span>

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              TCP/UDP
            </span>

          </div>

          <div
            class="flex items-center gap-2
            text-sm text-[var(--sec)]
            transition-transform duration-300
            group-hover:translate-x-1"
          >
            <span>Open simulator</span>
            <span class="text-lg">→</span>
          </div>

        </div>

      </div>

    </div>

  </a>
</div>


<!-- ========================================================= -->
<!-- PROJECT 06 — CPU SIMULATOR -->
<!-- ========================================================= -->

<div
  class="group relative mt-10 overflow-hidden rounded-[28px]
  border border-white/[0.12]
  bg-white/[0.045] backdrop-blur-2xl
  backdrop-saturate-150
  transition-all duration-500
  hover:border-[var(--sec)]/40
  hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
>

  <div
    class="pointer-events-none absolute -right-24 -bottom-24
    h-72 w-72 rounded-full
    bg-[var(--sec)] opacity-[0.045] blur-3xl
    transition-opacity duration-500
    group-hover:opacity-[0.09]"
  ></div>

  <a
    href="/projects/Cpu"
    class="relative block p-7 md:p-10"
  >

    <div class="flex flex-col md:flex-row gap-8">

      <div class="flex flex-1 flex-col justify-between">

        <div>

          <div class="mb-5 flex items-center gap-3">

            <span
              class="font-mono text-xs tracking-[0.2em]
              text-[var(--sec)]"
            >
              PROJECT 06
            </span>

            <span
              class="h-px w-12 bg-[var(--sec)] opacity-40"
            ></span>

            <span
              class="text-xs uppercase tracking-widest
              text-[var(--white-icon)]"
            >
              Computer Architecture
            </span>

          </div>

          <h3
            class="text-2xl md:text-4xl font-medium
            tracking-tight text-[var(--white)]
            transition-colors duration-300
            group-hover:text-[var(--sec)]"
          >
            CPU Simulator
            <span class="text-[var(--sec)]">.</span>
          </h3>

          <p
            class="mt-4 max-w-2xl text-base md:text-lg
            leading-relaxed text-[var(--white-icon)]"
          >
            A tiny 8-register CPU with its own instruction set,
            stepping through fetch, decode and execute one
            instruction at a time &mdash; write a short program
            and watch the registers and memory change live.
          </p>

          <div
            class="mt-7 flex flex-wrap items-center gap-x-6 gap-y-4"
          >

            <div>

              <p
                class="text-xs uppercase tracking-widest
                text-[var(--white-icon)] opacity-60"
              >
                Covers
              </p>

              <p
                class="mt-1 text-lg font-medium
                text-[var(--white)]"
              >
                Fetch-Decode-Execute &middot; Registers &middot; Memory
              </p>

            </div>

          </div>

        </div>

        <div
          class="mt-8 flex flex-col sm:flex-row
          sm:items-center sm:justify-between gap-5"
        >

          <div class="flex flex-wrap gap-2">

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Astro
            </span>

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              TypeScript
            </span>

            <span
              class="rounded-full border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Assembly
            </span>

          </div>

          <div
            class="flex items-center gap-2
            text-sm text-[var(--sec)]
            transition-transform duration-300
            group-hover:translate-x-1"
          >
            <span>Open simulator</span>
            <span class="text-lg">→</span>
          </div>

        </div>

      </div>

    </div>

  </a>
</div>


<!-- ========================================================= -->
<!-- PROJECT 07 — EXPENSE SPLITTER / FLUTTER APP -->
<!-- ========================================================= -->

<div
  class="group relative mt-10 overflow-hidden rounded-[28px]
  border border-white/[0.12]
  bg-white/[0.045] backdrop-blur-2xl
  backdrop-saturate-150
  transition-all duration-500
  hover:border-[var(--sec)]/40
  hover:shadow-[0_0_40px_rgba(21,128,61,0.1)]"
>

  <div
    class="pointer-events-none absolute -left-24 -top-24
    h-72 w-72 rounded-full
    bg-[var(--sec)] opacity-[0.045] blur-3xl
    transition-opacity duration-500
    group-hover:opacity-[0.09]"
  ></div>

  <!--
    Expense Splitter is a Flutter web build, deployed as static
    files at public/projects/expense-splitter/ in this repo.
    Build it with:
      flutter build web --release --base-href /projects/expense-splitter/
    then copy the contents of build/web/ into
    public/projects/expense-splitter/ so this href resolves.
  -->

  <a
    href="/projects/expense-splitter/"
    class="relative block p-7 md:p-10"
  >

    <div class="flex flex-col md:flex-row gap-8">

      <div class="flex flex-1 flex-col justify-between">

        <div>

          <div class="mb-5 flex items-center gap-3">

            <span
              class="font-mono text-xs tracking-[0.2em]
              text-[var(--sec)]"
            >
              PROJECT 07
            </span>

            <span
              class="h-px w-12 bg-[var(--sec)] opacity-40"
            ></span>

            <span
              class="text-xs uppercase tracking-widest
              text-[var(--white-icon)]"
            >
              Flutter App
            </span>

          </div>

          <h3
            class="text-2xl md:text-4xl font-medium
            tracking-tight text-[var(--white)]
            transition-colors duration-300
            group-hover:text-[var(--sec)]"
          >
            Expense Splitter
            <span class="text-[var(--sec)]">.</span>
          </h3>

          <p
            class="mt-4 max-w-2xl text-base md:text-lg
            leading-relaxed text-[var(--white-icon)]"
          >
            A Flutter mobile application for splitting group expenses,
            calculating balances between participants and minimizing
            the number of transfers needed to settle shared costs.
          </p>

          <div
            class="mt-7 flex flex-wrap items-center gap-x-6 gap-y-4"
          >

            <div>

              <p
                class="text-xs uppercase tracking-widest
                text-[var(--white-icon)] opacity-60"
              >
                Platform
              </p>

              <p
                class="mt-1 text-lg font-medium
                text-[var(--white)]"
              >
                Android &middot; iOS &middot; Web
              </p>

            </div>

            <div
              class="hidden sm:block h-8 w-px
              bg-[var(--white-icon-tr)]"
            ></div>

            <div>

              <p
                class="text-xs uppercase tracking-widest
                text-[var(--white-icon)] opacity-60"
              >
                Technology
              </p>

              <p
                class="mt-1 text-lg font-medium
                text-[var(--white)]"
              >
                Flutter &middot; Dart
              </p>

            </div>

          </div>

        </div>

        <div
          class="mt-8 flex flex-col sm:flex-row
          sm:items-center sm:justify-between gap-5"
        >

          <div class="flex flex-wrap gap-2">

            <span
              class="rounded-full
              border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Flutter
            </span>

            <span
              class="rounded-full
              border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Dart
            </span>

            <span
              class="rounded-full
              border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Mobile
            </span>

            <span
              class="rounded-full
              border border-[var(--white-icon-tr)]
              px-3 py-1 text-xs font-mono
              text-[var(--white-icon)]"
            >
              Expense Management
            </span>

          </div>

          <div
            class="flex items-center gap-2
            text-sm text-[var(--sec)]
            transition-transform duration-300
            group-hover:translate-x-1"
          >
            <span>Open Flutter app</span>

            <span class="text-lg">
              →
            </span>
          </div>

        </div>

      </div>

    </div>

  </a>
</div>
```

  </div>
</section>


<style>
/* ============================================================= */
/* LIQUID GLASS — PROJECTS                                      */
/* ============================================================= */

#projects {
  position: relative;
  isolation: isolate;
  --glass-border: rgba(255, 255, 255, 0.13);
  --glass-highlight: rgba(255, 255, 255, 0.12);
  --glass-shadow: rgba(0, 0, 0, 0.32);
  --glass-green: var(--sec);
}

/* Soft ambient light behind the whole collection. */
#projects::before {
  content: "";
  position: absolute;
  inset: 8% 4% 4%;
  z-index: -2;
  pointer-events: none;
  background:
    radial-gradient(circle at 12% 18%, rgba(21, 128, 61, 0.14), transparent 28%),
    radial-gradient(circle at 88% 52%, rgba(74, 222, 128, 0.08), transparent 26%),
    radial-gradient(circle at 50% 100%, rgba(15, 81, 50, 0.16), transparent 38%);
  filter: blur(42px);
}

/* Premium glass card base. */
#projects > .max-w-6xl > div > div[class*="rounded"] {
  position: relative;
  overflow: hidden;
  border: 1px solid var(--glass-border);
  background:
    linear-gradient(
      135deg,
      rgba(255,255,255,.075) 0%,
      rgba(255,255,255,.025) 38%,
      rgba(7,20,13,.38) 100%
    );
  box-shadow:
    inset 0 1px 0 var(--glass-highlight),
    inset 0 -1px 0 rgba(0,0,0,.12),
    0 24px 70px var(--glass-shadow);
  -webkit-backdrop-filter: blur(26px) saturate(145%);
  backdrop-filter: blur(26px) saturate(145%);
  transition:
    transform 500ms cubic-bezier(.2,.8,.2,1),
    border-color 400ms ease,
    box-shadow 500ms ease,
    background 500ms ease;
}

/* Glass reflection running across the top-left edge. */
#projects > .max-w-6xl > div > div[class*="rounded"]::before {
  content: "";
  position: absolute;
  inset: 1px;
  z-index: 0;
  pointer-events: none;
  border-radius: inherit;
  background:
    linear-gradient(
      115deg,
      rgba(255,255,255,.11),
      transparent 20%,
      transparent 62%,
      rgba(255,255,255,.025) 100%
    );
  opacity: .75;
}

/* Fine liquid-glass grain/grid. */
#projects > .max-w-6xl > div > div[class*="rounded"]::after {
  content: "";
  position: absolute;
  inset: 0;
  z-index: 0;
  pointer-events: none;
  border-radius: inherit;
  opacity: .22;
  background-image:
    linear-gradient(rgba(255,255,255,.025) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255,255,255,.025) 1px, transparent 1px);
  background-size: 36px 36px;
  mask-image: radial-gradient(circle at 72% 20%, black, transparent 72%);
  -webkit-mask-image: radial-gradient(circle at 72% 20%, black, transparent 72%);
}

/* Keep actual card content above decorative layers. */
#projects > .max-w-6xl > div > div[class*="rounded"] > a,
#projects > .max-w-6xl > div > div[class*="rounded"] > div {
  position: relative;
  z-index: 1;
}

/* Interactive lift. */
#projects > .max-w-6xl > div > div[class*="rounded"]:hover {
  transform: translateY(-7px);
  border-color: rgba(190, 242, 214, 0.28);
  background:
    linear-gradient(
      135deg,
      rgba(255,255,255,.105) 0%,
      rgba(255,255,255,.035) 38%,
      rgba(7,25,15,.48) 100%
    );
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.16),
    inset 0 -1px 0 rgba(0,0,0,.14),
    0 34px 90px rgba(0,0,0,.38),
    0 0 55px rgba(21,128,61,.10);
}

/* Moving green light that makes the glass feel fluid. */
#projects > .max-w-6xl > div > div[class*="rounded"] .group-hover\:opacity-\[0\.08\] {
  filter: blur(42px);
  transition:
    opacity 600ms ease,
    transform 800ms cubic-bezier(.2,.8,.2,1);
}

#projects > .max-w-6xl > div > div[class*="rounded"]:hover
  .group-hover\:scale-110 {
  transform: scale(1.14);
}

/* Heading polish. */
#projects h2 {
  letter-spacing: -0.055em;
  text-shadow: 0 1px 22px rgba(0,0,0,.25);
}

#projects h2 .text-\[var\(--sec\)\] {
  text-shadow: 0 0 24px rgba(21,128,61,.42);
}

/* Project numbers become subtle glass metadata. */
#projects .font-mono.text-xs {
  text-shadow: 0 0 18px rgba(21,128,61,.25);
}

/* Technology chips: translucent capsules. */
#projects .rounded-full {
  background: rgba(255,255,255,.035);
  border-color: rgba(255,255,255,.12);
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.07),
    inset 0 -1px 0 rgba(0,0,0,.08);
  -webkit-backdrop-filter: blur(10px);
  backdrop-filter: blur(10px);
  transition:
    background 250ms ease,
    border-color 250ms ease,
    transform 250ms ease;
}

#projects .rounded-full:hover {
  background: rgba(190,242,214,.075);
  border-color: rgba(190,242,214,.25);
  transform: translateY(-1px);
}

/* Weather subcards get their own nested glass layer. */
#projects .rounded-xl {
  border-color: rgba(255,255,255,.105);
  background:
    linear-gradient(
      145deg,
      rgba(255,255,255,.065),
      rgba(255,255,255,.018)
    );
  box-shadow:
    inset 0 1px 0 rgba(255,255,255,.08),
    0 12px 35px rgba(0,0,0,.15);
  -webkit-backdrop-filter: blur(18px) saturate(135%);
  backdrop-filter: blur(18px) saturate(135%);
}

/* Tiny live-status glow for the weather project. */
#projects .rounded-xl:has([id^="weather-"])::before {
  content: "";
  position: absolute;
  top: 14px;
  right: 14px;
  width: 6px;
  height: 6px;
  border-radius: 50%;
  background: var(--glass-green);
  box-shadow: 0 0 14px rgba(21,128,61,.75);
  opacity: .85;
}

/* CTA arrow has a glassy green glow. */
#projects .group:hover .text-\[var\(--sec\)\] {
  text-shadow: 0 0 18px rgba(21,128,61,.34);
}

/* Better focus accessibility. */
#projects a:focus-visible {
  outline: 2px solid rgba(190,242,214,.75);
  outline-offset: 5px;
  border-radius: 18px;
}

/* Mobile: keep the effect but reduce blur cost. */
@media (max-width: 768px) {
  #projects > .max-w-6xl > div > div[class*="rounded"] {
    border-radius: 22px;
    -webkit-backdrop-filter: blur(20px) saturate(135%);
    backdrop-filter: blur(20px) saturate(135%);
  }

  #projects > .max-w-6xl > div > div[class*="rounded"]:hover {
    transform: translateY(-3px);
  }

  #projects > .max-w-6xl > div > div[class*="rounded"]::after {
    background-size: 30px 30px;
    opacity: .13;
  }
}

/* Respect users who disable motion. */
@media (prefers-reduced-motion: reduce) {
  #projects > .max-w-6xl > div > div[class*="rounded"],
  #projects .rounded-full,
  #projects h3 {
    transition: none !important;
  }

  #projects > .max-w-6xl > div > div[class*="rounded"]:hover,
  #projects .rounded-full:hover {
    transform: none !important;
  }
}
</style>

<script>
  interface WeatherData {
    current: {
      temperature_2m: number;
      weather_code: number;
    };
  }

  interface WeatherLocation {
    latitude: number;
    longitude: number;
    temperatureElement: string;
    statusElement: string;
  }

  const weatherLocations: WeatherLocation[] = [
    {
      latitude: 38.6983,
      longitude: -0.4743,
      temperatureElement: "weather-alcoi-temperature",
      statusElement: "weather-alcoi-status",
    },
    {
      latitude: 39.4699,
      longitude: -0.3763,
      temperatureElement: "weather-valencia-temperature",
      statusElement: "weather-valencia-status",
    },
  ];

  function getWeatherDescription(code: number): string {
    if (code === 0) return "Clear";
    if (code === 1) return "Mainly clear";
    if (code === 2) return "Partly cloudy";
    if (code === 3) return "Cloudy";
    if (code === 45 || code === 48) return "Fog";
    if (code >= 51 && code <= 57) return "Drizzle";
    if (code >= 61 && code <= 67) return "Rain";
    if (code >= 71 && code <= 77) return "Snow";
    if (code >= 80 && code <= 82) return "Showers";
    if (code >= 85 && code <= 86) return "Snow showers";
    if (code >= 95 && code <= 99) return "Thunderstorm";

    return "Unknown";
  }

  async function loadWeather(location: WeatherLocation) {
    const temperatureElement = document.getElementById(
      location.temperatureElement
    );

    const statusElement = document.getElementById(
      location.statusElement
    );

    if (!temperatureElement || !statusElement) return;

    try {
      const url =
        `https://api.open-meteo.com/v1/forecast` +
        `?latitude=${location.latitude}` +
        `&longitude=${location.longitude}` +
        `&current=temperature_2m,weather_code` +
        `&timezone=Europe%2FMadrid`;

      const response = await fetch(url);

      if (!response.ok) {
        throw new Error("Weather request failed");
      }

      const data = (await response.json()) as WeatherData;

      const temperature = data.current.temperature_2m;
      const weatherCode = data.current.weather_code;

      temperatureElement.textContent =
        `${Math.round(temperature)}°C`;

      statusElement.textContent =
        getWeatherDescription(weatherCode);

    } catch {
      temperatureElement.textContent = "--°C";
      statusElement.textContent = "Unavailable";
    }
  }

  function loadAllWeather() {
    weatherLocations.forEach((location) => {
      void loadWeather(location);
    });
  }

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", loadAllWeather);
  } else {
    loadAllWeather();
  }
</script>
