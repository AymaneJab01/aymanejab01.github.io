import 'package:flutter/material.dart';

/// Central color palette. Change a value here and it updates everywhere —
/// this is the only place colors should be hard-coded in the app.
class AppColors {
  AppColors._();

  static const Color darkGreen = Color(0xFF1F2B1A);
  static const Color forest = Color(0xFF2A3B22);
  static const Color olive = Color(0xFF9CCB3B);
  static const Color oliveDark = Color(0xFF7FA82E);
  static const Color cream = Color(0xFFF4F2EA);
  static const Color card = Color(0xFFFFFFFF);
  static const Color mutedText = Color(0xFF8A8F84);
  static const Color danger = Color(0xFFE0644B);
  static const Color success = Color(0xFF5FA85A);
  static const Color divider = Color(0xFFE7E5DC);
}
